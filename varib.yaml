
- name: var_files
  hosts: local
  connection: local
  vars_prompt: 
  - name: username
    prompt: enter your username
    private: true

  - name: password
    prompt: enter your password
    private: true


  tasks:
  - name: trainer  info
    ansible.builtin.debug:
     msg: " Trainer details is {{ username }} and age is {{ password }}" 

  #  - name: trainer  info
  #   ansible.builtin.debug:
  #    msg: " Trainer details is {{ Trainer }} and age is {{ AGE }}" 